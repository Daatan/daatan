# Gemini / agent guidance

This file is loaded automatically by Gemini CLI (and other GEMINI.md-aware agents) when working in this repo. Keep it terse; it's read on every turn.

This repo's canonical agent guidance lives in [`CLAUDE.md`](./CLAUDE.md) — hard rules (interview-before-implementing, PR-only workflow, version bumps, tag-triggered deploys, migrations, pgvector), common patterns, code style, the where-to-look index, and the infra cheat-sheet all apply identically to Gemini CLI. Read it.

For the production release checklist (Claude Code runs it via the `/release` skill), read and follow [`.claude/commands/release.md`](./.claude/commands/release.md) directly — same steps, one copy to keep in sync.
